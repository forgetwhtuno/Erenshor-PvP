namespace ErenshorPvP
{
    // Production launcher migrated to retained uGUI in PvpPanel. This type remains only so
    // older source-level references fail loudly if reintroduced rather than silently reviving IMGUI.
    internal sealed class PvpLauncher { }
}
