namespace ErenshorPvP
{
    // Legacy source-file name retained for repository compatibility. Production position
    // ownership moved to PvpUiGeometry + RectTransform; no top-origin pixel migration occurs.
    internal static class PvpPanelPositioning
    {
        internal static string RunSelfTests() { return PvpUiGeometry.RunSelfTests(); }
    }
}
